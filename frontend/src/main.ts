/**
 * main.ts — Ponto de entrada da aplicação
 *
 * A lógica de cada página é carregada via <script type="module"> diretamente
 * nos arquivos HTML, mantendo o bundle por página pequeno e independente.
 * Este arquivo serve apenas para importar os estilos globais.
 */

import './styles/style.css'
